[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA routing — send all paths to index.html
[[redirects]]
  from = "/api/*"
  to = "https://carsense-api-dp27.onrender.com/"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
